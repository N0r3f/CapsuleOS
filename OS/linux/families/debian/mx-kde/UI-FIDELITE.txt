Checklist fidélité MX Linux KDE — base initiale (2026-04)
=========================================================

Références visuelles
--------------------
- Les captures de référence sont stockées sous `visuel/mx-kde/` et `visuel/screen/calendar.png` (panel + popover calendrier Plasma).
- Ces assets servent à la comparaison visuelle, pas au runtime.

Objectifs de la base
--------------------
- [x] Nouvelle distro `mx-kde` dérivée de la branche Debian.
- [x] Identité propre (body id, titre, profil distro, branding desktop).
- [x] Shell KDE-like initial (panel + chrome fenêtre unifié).
- [x] Apps prioritaires alignées sur un style de contrôles cohérent.

Points à affiner
----------------
- [x] Ajustement pixel-perfect panel/clock/tray selon captures KDE.
- [x] Ajustement du launcher/menu principal pour coller au flux KDE.
- [x] Harmonisation des icônes applicatives avec pack de référence.
- [x] Passe finale sur espacement/ombres/rayons (fenêtres + popovers).

Panel et calendrier (2026-05)
-----------------------------
- [x] Panel Plasma : launcher MX, épingles pédagogiques, tray (terminal, presse-papiers, réglages, affichage, volume, réseau), horloge avec filet bleu à l’ouverture du calendrier, bande « afficher le bureau ».
- [x] Popover calendrier monocolonne : onglets Jours/Mois/Années (Mois/Années décoratifs), Aujourd’hui + navigation, grille lun.–dim. avec jours hors mois atténués.
- [x] Script local `js/calendar-popover-kde.js` (sans `kernel/js/calendar-popover.js` sur cette skin).

Correspondance icônes (panel / épingles)
----------------------------------------
- Launcher MX : `media/img/assets/mx-logo.png` (capture / branding local).
- Dolphin : `media/img/apps/org.kde.dolphin.svg` (pack apps MX-KDE).
- Firefox : `media/img/apps/firefox.svg` ; barre titre fenêtre plate (chrome Konsole) avec `media/img/header/go-down.svg`, `window-restore.svg`, `window-close.svg`.
- Writer / Paramètres système / Missions MX : même barre titre plate (`librewriter.skin.css`, `themes.skin.css`, `checklist.skin.css`) avec icônes `libreoffice-writer.svg`, `systemsettings.svg`, `mx-tools.svg`.
- Terminal (épingle + tray) : `media/img/apps/utilities-terminal.svg` ; tray : `media/img/panel/terminal.svg`.
- Writer : `media/img/apps/libreoffice-writer.svg`.
- Thèmes / réglages : `media/img/apps/systemsettings.svg` ; tray : `media/img/panel/settings.svg`.
- Checklist : `media/img/apps/mx-tools.svg`.
- Accueil CapsuleOS : `assets/accueil.svg` (racine site).
- Tray : `media/img/panel/clipboard.svg`, `display.svg`, `network.svg` ; volume via masque CSS dans `footer.css`.
- Calendrier (en-tête) : `media/img/panel/calendar-settings.svg`, `calendar-pin.svg`.

Terminal Konsole (2026-05)
--------------------------
- [x] Barre titre plate Breeze Dark ; contrôles fenêtre via `media/img/header/go-down.svg`, `window-restore.svg`, `window-close.svg` (assets Dolphin, chrome Konsole).
- [x] Titre fenêtre statique : `~ : bash — Konsole` (`content/strings.json`).
- [x] Barre d’outils décorative (onglet, scinder, copier désactivé, coller, rechercher, menu) ; gabarit partagé masqué hors MX-KDE.
- [x] Workspace sombre, prompt coloré (`js/terminal-konsole-chrome.js`), session `mx-linux@mx`.
- [x] FS terminal aligné sur `/home/user` (noyau `kernel/js/terminal/filesystem.js`) ; `ls` / `cd` / `cat` cohérents avec le home pédagogique.
- [x] Prompt Konsole recoloré après chaque commande (pas de cache DOM sur `textContent` noyau).

Chrome fenêtre Konsole (2026-05, MX-KDE uniquement)
---------------------------------------------------
- [x] Hauteur barre titre standard `2.05rem` (`--mx-kde-konsole-header-height` dans `style/style.css`) sur Firefox, Terminal, Writer, Paramètres, Missions, À propos et fenêtres sans skin dédié (`style/windows.css`) ; **Dolphin** conserve `1.12rem` (`style/apps/dolphin.skin.css`).
- [x] Tokens partagés : tailles icône/épingle/titre (`--mx-kde-header-icon-size`, `--mx-kde-header-pin-size`, `--mx-kde-header-title-size`) et filtres monochromes (`--mx-kde-mono-logo-filter`) pour logos noir-gris / blanc-gris (launcher panel, logo À propos, épingle titre) ; icônes d’app multicolores en barre titre sans inversion globale.
- [x] Contrôles fenêtre : `--mx-kde-header-control-filter` (sombre) / `--mx-kde-header-control-filter-light` (clair).

Menu démarrer Plasma (2026-05, MX-KDE uniquement)
-------------------------------------------------
- [x] Référence `visuel/screen/startup_menu.png` : shell 2 colonnes (rail icônes + catégories), recherche en bas avec focus bleu, fond sombre flouté (`style/apps/mainMenu.skin.css`, tokens `style/style.css`).
- [x] Données locales `content/mainMenu-data.js` (catégories MX, apps pédagogiques) ; placeholder `Rechercher...` dans `content/strings.json`.
- [x] Comportement Plasma : liste d’apps au clic catégorie ou recherche ; catégories décoratives atténuées ; retour catégories via `js/mainMenu-kde-chrome.js`.

Validation prévue
-----------------
- [x] Boot HTTP + file://
- [x] Drag/resize/focus des fenêtres
- [x] Terminal Debian cohérent (man/runtime)
- [x] Non-régression Mint/Ubuntu/Fedora
